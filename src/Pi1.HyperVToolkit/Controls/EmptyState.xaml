<UserControl x:Class="Pi1.HyperVToolkit.Controls.EmptyState"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,24">
        <TextBlock Text="—" FontSize="32" Foreground="#9E9E9E" HorizontalAlignment="Center" />
        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="15" FontWeight="SemiBold" Foreground="#616161"
                   HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" />
        <TextBlock Text="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Foreground="#9E9E9E" Margin="0,4,0,0"
                   HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="520" />
    </StackPanel>
</UserControl>
