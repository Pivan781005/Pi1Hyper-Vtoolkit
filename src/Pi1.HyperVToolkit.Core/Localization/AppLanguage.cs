namespace Pi1.HyperVToolkit.Core.Localization;

// UI presentation language. Persisted as a stable code ("sk"/"en") — never
// the localized display text.
public enum AppLanguage
{
    Slovak,
    English,
}
